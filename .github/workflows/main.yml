name: "RDP_Factory_Settings"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: "Fluxo_01: Checkout"
        uses: actions/checkout@v4

      - name: "Fluxo_02: Configuração do Usuário e RDP"
        shell: powershell
        run: |
          # 1. Cria o usuário com a senha do seu Secret
          $p = "${{ secrets.otimizacao2 }}"
          net user runneradmin $p
          
          # 2. Adiciona o usuário ao grupo de Acesso Remoto (Obrigatório para NLA)
          net localgroup "Remote Desktop Users" runneradmin /add
          
          # 3. Garante que o RDP esteja habilitado (Configuração de fábrica)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          
          # 4. Habilita explicitamente a NLA (Valor 1 = Ativada)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          
          # 5. Abre a porta no Firewall
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: "Fluxo_03: Iniciar Cloudflare"
        shell: powershell
        run: |
          # Baixa o executável
          Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "C:\cloudflared.exe"
          
          # Inicia o túnel
          Start-Process -FilePath "C:\cloudflared.exe" -ArgumentList "tunnel --url rdp://127.0.0.1:3389" -RedirectStandardError "v_log.txt"

      - name: "Fluxo_04: Link e Instruções"
        shell: powershell
        run: |
          $ep = ""
          while (!$ep) {
            Start-Sleep -Seconds 3
            if (Test-Path "v_log.txt") {
              $log = Get-Content "v_log.txt"
              $ep = $log | Select-String -Pattern "https://.*\.trycloudflare\.com" | ForEach-Object { $_.Matches.Value }
            }
          }
          $cleanLink = $ep.Replace("https://", "")
          Write-Host "----------------------------------------------"
          Write-Host "COPIE ESTE ENDEREÇO: $cleanLink"
          Write-Host "USUARIO: runneradmin"
          Write-Host "SENHA: (A que você definiu no Secret)"
          Write-Host "----------------------------------------------"
          Start-Sleep -Seconds 21600
