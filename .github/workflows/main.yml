name: "RDP_Fast_Install"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: "Fluxo_01: Setup Ultra Rápido"
        shell: powershell
        run: |
          # 1. Inicia o download em segundo plano (Ganho de tempo)
          $down = Start-Job -ScriptBlock {
            Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "C:\cloudflared.exe"
          }

          # 2. Configura senha e RDP enquanto o download acontece
          $p = "${{ secrets.otimizacao2 }}"
          net user runneradmin $p
          net localgroup "Remote Desktop Users" runneradmin /add
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

          # 3. Aguarda o término do download (se ainda não acabou)
          Wait-Job $down | Receive-Job

      - name: "Fluxo_02: Conexão Instantânea"
        shell: powershell
        run: |
          # Inicia o túnel imediatamente
          Start-Process -FilePath "C:\cloudflared.exe" -ArgumentList "tunnel --url rdp://127.0.0.1:3389" -RedirectStandardError "v_log.txt"
          
          # Loop de busca otimizado (espera menor entre tentativas)
          $ep = ""
          while (!$ep) {
            Start-Sleep -Seconds 2
            if (Test-Path "v_log.txt") {
              $log = Get-Content "v_log.txt"
              $ep = $log | Select-String -Pattern "https://.*\.trycloudflare\.com" | ForEach-Object { $_.Matches.Value }
            }
          }
          $cleanLink = $ep.Replace("https://", "")
          Write-Host "`n----------------------------------------------"
          Write-Host "LINK RDP: $cleanLink"
          Write-Host "USUARIO: runneradmin"
          Write-Host "----------------------------------------------"
          Start-Sleep -Seconds 21600
